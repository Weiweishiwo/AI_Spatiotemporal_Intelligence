# ===== backend / common =====
fastapi
uvicorn
# uvicorn 的 WebSocket 支持（/ws/track 实时回放 + pytest 测试都需要）
websockets
pydantic-settings
sqlmodel
pymysql
httpx

# ===== data / simulation =====
pandas
numpy
pyproj

# ===== spatiotemporal intelligence =====
networkx

# ===== perception (member 1; CPU-only torch on Windows by default, no NVIDIA needed) =====
ultralytics
opencv-python

# ===== agents =====
openai

# ===== tests =====
pytest

# Note: LabelImg (data annotation GUI) must be installed separately; MySQL 8 is installed locally, not via pip.
